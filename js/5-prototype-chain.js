/* function Persona(nombre) {
    this.nombre = nombre;

    this.correr = function() {
        console.log(this.nombre + ' está corriendo');
    }
}

Persona.prototype.trotar = function () {
    console.log(this.nombre + ' está trotando');
}

const objeto = new Persona('manuel');

// cadena de prototipos

console.log(objeto);

objeto.correr();
objeto.correr(); */