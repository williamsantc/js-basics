/* function calculo(x) {
    let t = 10;
    x += 3;

    return function (y) {
        return x + y + t;
    }
}

const val = calculo(4);

console.log(val);

const resultado = val(2);

console.log(resultado); */

// Estandar - EcmaScript

// implementaciones del estandar (V8, spider monkey)